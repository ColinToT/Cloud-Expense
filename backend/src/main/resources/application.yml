spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cloud_expense
    username: app_user
    password: 123456

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: false

logging:
  level:
    org.springframework.security: DEBUG